import React from 'react';

const AreaDisplay = props => {
  
  const first = props.zipcode[props.genres[0]];

  return (
    <div>
      Yoohoo {proptszipcodeText}
    </div>
  )
}

export default AreaDisplay;